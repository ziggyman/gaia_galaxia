rm csvData.so
rm ../python/csvData.so
g++ -O3 -shared -std=c++11 -undefined dynamic_lookup `python2 -m pybind11 --includes` csvData.cc ../python/csvData.cc -I/opt/local/include -L/usr/local/lib -Wno-c++11-extensions -o csvData.so
cp csvData.so ../python/
